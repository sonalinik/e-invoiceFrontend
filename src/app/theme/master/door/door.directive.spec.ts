import { DoorDirective } from './door.directive';

describe('DoorDirective', () => {
  it('should create an instance', () => {
    const directive = new DoorDirective();
    expect(directive).toBeTruthy();
  });
});
